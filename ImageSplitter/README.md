# Image Grid Splitter - PyQt6

A simple Python tool to split an image into a grid of smaller images using a GUI built with PyQt6.

## Features

Select any image (JPG, PNG, BMP).

Specify the number of rows and columns for the grid.

Choose the output folder to save the split images.

Saves all cropped images as PNG files with sequential numbering.